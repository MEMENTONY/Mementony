# Memento — 폴리마켓 거래복기 · 리스크 관리 도구

내 폴리마켓 베팅을 **이성적으로, 덜 잃도록** 돕는 개인용 도구입니다.
핵심 목표는 "수익 극대화"가 아니라 **계좌 생존**(감정적 큰 손실 방지) + **거래복기로 배우기**.

> ⚠️ **Python 3.12 이상 필요** (코드가 3.12 문법을 사용). Streamlit Cloud로 배포 중.

---

## 🗂️ 파일이 하는 일 (제일 중요)

이 앱은 한 덩어리였던 코드를 **역할별 파일**로 나눠 두었습니다. 각 파일이 무슨 일을 하는지:

| 파일 | 한 줄 설명 | 자세히 |
|------|-----------|--------|
| **`streamlit_app.py`** | 앱 본체 (화면 흐름) | 페이지 설정 → 디자인(CSS) → 상태 불러오기(**시트 자동 복원 포함**) → **지갑 전체 자동 동기화**(포지션·장부·승/패 자동확정) → 사이드바(오늘 운용) → **5개 탭**(진입판독 · 포트폴리오 · 기록(거래일지/거래복기) · AI리서치 · 설정). 다른 파일의 기능들을 불러와 화면에 배치합니다. |
| **`config.py`** | 설정·상수 | 기본값(`DEFAULTS`), **저장할 항목 목록**(`PERSIST_KEYS`), 기본 리스크 프로필, 카테고리 분류 키워드 등. |
| **`state.py`** | 저장 / 불러오기 | 내 데이터(거래·설정·장부)를 `memento_state.json` 파일에 **자동 저장**하고 다시 불러옵니다. |
| **`ui.py`** | 공용 표시 도구 | 다국어(`t`), 금액 표시(`money`), 카드·미터·라벨 같은 **작은 화면 조각**을 만드는 기본 도구. |
| **`engine.py`** | 계산 두뇌 🧠 | 진입 판독(`calculate_entry`), 켈리 사이징, **손실 한도·틸트·잠금**, 손익 계산(`group_auto_trades_for_pnl`), **승/패 확정**(`resolve_trade_row`), **거래장부**(`update_trade_ledger`), 성과(`performance_summary`), **감정·행동 인사이트**(`behavior_insights`), **규칙 시뮬레이터**(`rule_simulation`), **자산 곡선·캘린더·주간 리포트**(`equity_curve_data`/`calendar_heatmap_data`/`weekly_report`). |
| **`data.py`** | 외부 통신·데이터 정리 | 폴리마켓 지갑·시세 가져오기(`fetch_*`), **승/패 자동 확정**(`auto_resolve_trades`), Claude AI 호출(`call_claude`), **구글시트 양방향**(장부 백업/가져오기 + **전체 상태 스냅샷 백업/복원**). |
| **`views.py`** | 화면 렌더 | 진입 판독 결과(`render_entry_result`), 거래 손익 카드(`render_trade_pnl_summary`), 성과·**행동 인사이트·규칙 시뮬레이터·자산 곡선·주간 리포트** 렌더, **지갑 동기화 단일 파이프라인**(`sync_wallet_full`). |
| **`tests/`** | 자동 테스트 | 부팅·계산·동기화·백업/복원 검증 8종. **PR마다 GitHub Actions(CI)가 자동 실행** — 초록불이어야 머지. |

**불러오는 순서(의존 방향):** `config` → `state` → `ui` → `engine` → `data` → `views` → `streamlit_app`
(위에서 아래로만 의존하므로 서로 꼬이지(순환참조) 않습니다.)

---

## 🔧 "이걸 바꾸려면 어디를 봐야 하지?" 빠른 안내

| 바꾸고 싶은 것 | 볼 파일 |
|----------------|---------|
| 진입 판독 점수·규칙 (켈리, 손실한도, 틸트 등) | `engine.py` (`calculate_entry`) |
| 손익 계산 방식 / 승·패 처리 | `engine.py` (`group_auto_trades_for_pnl`, `resolve_trade_row`) |
| 승패·카테고리 성과 집계 | `engine.py` (`performance_summary`) |
| 화면 문구·배치·탭 구성 | `streamlit_app.py`, `views.py` |
| 색·폰트·디자인 | `streamlit_app.py` 맨 위 CSS 블록 |
| 기본값·저장할 항목 | `config.py` (`DEFAULTS`, `PERSIST_KEYS`) |
| 폴리마켓·AI 연결 | `data.py` |

---

## 💾 내 데이터는 어디에?

- 모든 거래·설정·**거래장부**는 `memento_state.json`(로컬 파일)에 **매 실행마다 자동 저장**됩니다.
- **구글시트 이중 백업**: 거래장부는 시트 `ledger` 탭으로(변경마다 자동), 전체 상태는 `state` 탭으로 백업됩니다. Streamlit Cloud가 **재배포되어 로컬 파일이 초기화되면 부팅 때 시트에서 자동 복원** — Apps Script URL을 Streamlit **Secrets**(`gsheet_webapp_url`)에 넣어두면 항상 동작합니다.
- 시트에서 고친 **카테고리·감정(1-5)·추격(Y)** 은 설정 탭 "시트에서 가져오기"로 앱에 반영됩니다.
- 지갑 주소를 한 번 저장해두면, 앱을 열 때 **전체 자동 동기화**(포지션·거래·승/패 자동확정, 중복 없이)가 실행됩니다.
- 한 번 장부에 들어온 거래는 **폴리마켓에서 사라져도 보존**됩니다. (거래일지 탭에서 CSV로 내려받기 가능)

---

## ▶️ 실행 방법

```bash
# 1) 라이브러리 설치
pip install -r requirements.txt

# 2) 실행
streamlit run streamlit_app.py
```

---

## 🧩 주요 기능 요약

- **진입 판독** — 진입 전 리스크 점검 + 켈리 추천 베팅액
- **생존 가드레일** — 일일 손실 한도 차단 · 연속 손실(틸트) 차단 · 손실 연동 베팅 축소 · "오늘 그만" 잠금
- **거래일지** — 전체 자동 동기화(포지션·거래·**승/패 자동확정**) + 손익 계산 + 감정(1~5)·추격 태그 + 승패·카테고리 성과 + **자산 곡선(MDD)·일별 손익 캘린더**
- **거래복기** — **주간 리포트**(이번 주 vs 지난주) + **행동 인사이트**(감정×손익·추격·규칙위반 비용) + **규칙 시뮬레이터**("지켰다면 얼마였나") + 거래별 복기 노트
- **거래장부** — 영구 보존 + CSV + **구글시트 양방향** + 재배포에도 살아남는 **전체 상태 백업/복원**
- **AI 리서치** — Claude로 시장 해석·체크리스트 (선택)
- **테스트 + CI** — `tests/` 8종, PR마다 자동 실행 (`python 3.12` 필수: `for f in tests/test_*.py; do python "$f"; done`)
